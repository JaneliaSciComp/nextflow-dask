includeConfig 'nextflow.config'

profiles {

    standard {
        docker {
            enabled = true
        }

        singularity {
            enabled = false
        }
    }

}

docker {
    temp = "auto"
    runOptions = "--platform=linux/x86_64 ${default_docker_runtime_opts}"
}
